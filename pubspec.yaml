name: thisdatedoesnotexist
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^5.0.1
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2
  firebase_core: ^2.21.0
  firebase_auth: ^4.12.1
  mobx: ^2.2.1
  flutter_mobx: ^2.1.1
  flutter_modular: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.2
  build_runner: ^2.4.6
  flutter_lints: ^3.0.1
  lints: ^3.0.0
  mobx_codegen: ^2.4.0

scripts:
  mobx: fvm dart pub run build_runner build --delete-conflicting-outputs

flutter:
  uses-material-design: true